#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> ipair;

void solve() {}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  int T;
  cin >> T;
  for (int t = 1; t <= T; t++) {
    int R, C;
    vector<vector<int>> g;
    
    solve();
    // cout << "Case #" << t << ": " << ans << "\n";
  }
  return 0;
}
